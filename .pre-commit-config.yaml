repos:
  - repo: local
    hooks:
      - id: architecture
        name: Check architecture snapshot
        entry: python3 scripts/check_architecture.py --staged
        language: system
        always_run: true
        pass_filenames: false
