---
- name: Destroy | Stop and remove Drover containers
  become: true
  community.docker.docker_container:
    name: "{{ item.value.container_name }}"
    state: absent
  loop: "{{ drover_services | dict2items }}"
  when: inventory_hostname in groups['drover']
