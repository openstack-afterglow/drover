# Drover 작업 규칙

## Architecture maintenance

- 작업 전에 루트 [`ARCHITECTURE.md`](ARCHITECTURE.md)와 영향을 받는 `docs/` 상세 문서를 읽는다.
- code/config/schema/dependency/deploy/test 변경은 같은 변경에서 영향을 받는 architecture 본문과 source-linked 상세 문서를 갱신한다. 구조 영향이 없는 bugfix/refactor도 최신 review summary에 그 이유를 남긴다.
- source가 문서보다 우선한다. 계획·roadmap·이전 문구를 현재 구현의 증거로 승격하지 않는다.
- 실제 source를 검토한 뒤 `python3 scripts/check_architecture.py --stamp --summary "변경 경로와 영향"`를 실행한다. 부분 staging을 검토할 때는 `--stamp --staged --summary "..."`를 사용한다.
- 완료 또는 commit 전 `python3 scripts/check_architecture.py`를 통과시킨다. 이 저장소는 `python3 scripts/check_architecture.py --staged` pre-commit hook도 사용한다.
- credentials, raw token, password, encryption key를 문서·로그·예제에 기록하지 않는다.

## 검증 진술

`uv run pytest ...` 또는 live OpenStack 호출을 실제 실행하지 않았다면 문서에 `test-passed`/`live-verified`라고 쓰지 않는다. 테스트 파일 존재는 `test-defined`로만 기록한다. 일반 검증 진입점은 `uv run pytest tests`, `uv run ruff check .`, `uv run drover-migrate --apply`이며 외부 DB/Redis/Keystone 전제조건을 명시한다.

## CI 파이프라인 성능 규정 (critical-path first)

근거: 2026-09 Drover GitHub Actions 실측(아래 기준선)과 afterglow CI 개편 경험. `.github/workflows/`, CI에서 실행되는 테스트, `docker/Dockerfile`을 바꾸는 변경은 아래 규칙을 따른다. Drover는 OpenSpec을 쓰지 않으므로 전후 수치는 커밋 본문(또는 PR)과 이 절의 기준선에 남긴다.

### 기준선과 현재 형태

- 개선 전 기준선(2026-08-30 → 2026-09-19): `CI`(`ci.yml`) 크리티컬 패스(실행 생성 → 마지막 잡 종료) 중앙값 100초, p90 151초(n=40, push 30건 + PR 10건 혼합, runs 33339073898..35421775251). `Docker Build & Push`(`docker-build.yml`, reusable `test` + `build-and-push`) 중앙값 160초, p90 231초(n=40, runs 33358781597..35421775411).
- 같은 기간 이벤트별 기준선(개선 후와 같은 조건으로 비교할 값): `CI` PR만 최근 20건 중앙값 105초(104.5), p90 148초(148.1)(n=20, runs 33299439644..35405417634, 2026-08-30 07:32Z → 2026-09-18; 혼합 창 이전의 2026-08-30 PR 10건을 포함하며 이전 n=10 기록은 105.5/152.9초였다). 이 20건의 마지막 잡은 `docker-build-and-scan` 16/20, `service` 4/20이고, 기간 중 `ci.yml`은 같은 5잡 구성에서 package 잡 이름만 바뀌었다(그 잡이 마지막으로 끝난 적은 없다). `CI` push만 중앙값 98초, p90 144초(143.5)(n=30, runs 33339073898..35421775251). `Docker Build & Push` push 실행의 suite 구간(실행 생성 → 마지막 `test / *` 잡 종료) 중앙값 99초(98.5), p90 123초(122.6)(n=32, runs 33358781597..35421775411).
- 잡 중앙값: `docker-build-and-scan` 94초(p90 148초, 느린 실행에서 apt-get 두 단계가 50-110초), `service` 90초(pytest 78초), `db-migration-and-readiness` 39초(Initialize containers 23초), `sdk` 10초, `package-wheel` 9초. 잡 대기열 중앙값 2초. 마지막으로 끝나는 잡은 `docker-build-and-scan` 25/40, `service` 15/40.
- 2026-09 개선: push와 PR의 중복 suite 실행 제거, cert rotation 운영 코드(`drover/services/cert_rotation.py`)의 고정 10초 sleep 제거(로컬 pytest 78초 → 11-18초), 스캔 잡의 docker driver 빌드, 서비스 health-check 2초 interval(절감 미확인, 아래 고정비 항목과 4번 규칙; db 잡은 마지막 잡 0/40이라 크리티컬 패스 투영치에 넣지 않았다). 투영치(실측 전)는 혼합 `CI` 기준선 중앙값 100초 → 약 82초이고, p90은 Debian apt 미러 지연 때문에 약 150초로 유지될 수 있다(회귀 아님). 이벤트별 기준선에 대한 투영치는 따로 계산하지 않았다.
- 잡당 고정비 실측(4번 규칙, 기준선 창 40회 스텝 중앙값, 1초 해상도): `setup-uv` 2초, `uv sync --all-extras --frozen` `service` 3초·`db-migration-and-readiness` 4초·`sdk` 1초, db 잡 `Initialize containers` 23초(p90 30초, service image pull 포함). job log 4건(hit: job 105794073559·105291927609·105794061515, miss: job 105291927868)에서 setup-uv 기본 `prune-cache: true`로 저장된 uv cache는 약 93-97KB였고, hit과 miss 모두 `uv sync`가 `Prepared 81 packages`(2.2-4.5초)를 출력했다. 즉 캐시는 wheel을 재사용하지 않고 복원에 0.1-0.6초가 든다. 복원이 재설치보다 느리지 않으므로 4번 규칙의 제거 조건은 아니어서 `enable-cache: true`를 유지하며, 이득이 필요하면 `prune-cache: false`를 실측 비교한 뒤 바꾼다.
- 개선 후 측정 방법: `CI`는 이제 PR과 `workflow_dispatch`에서만 실행된다. PR 실행은 몰려서 생겼으므로(위 20건 중 10건이 2026-08-30 하루, 2026-09-02 이후 17일간 3건) 20회 표본이 쌓이는 시점은 추정하지 않는다. 개선 후 suite 크리티컬 패스는 (a) `CI` PR 실행의 실행 생성 → 마지막 잡 종료와 (b) `Docker Build & Push` push/tag 실행의 실행 생성 → 마지막 `test / *` 잡 종료로 나눠 재고, 각각 위 이벤트별 기준선(`CI` PR 104.5/148초, Docker push suite 구간 99/123초)과 비교한다. 혼합 기준선(100/151초)과의 비교는 참고용이다. 20회 이상 쌓이면 이 절에 전후 수치를 추가한다.
- 현재 형태: main/dev 대상 PR(fork·dependabot 포함)은 `CI`를 한 번 실행한다. dev/main push와 `v*` tag는 `Docker Build & Push`의 `test`(`ci.yml` workflow_call)를 한 번 실행하고, `build-and-push`가 `needs: test`로 그 전체 결과를 기다린다. `CI`는 `workflow_dispatch`로 발행 없이 수동 실행할 수 있다.
- 수용한 검증 공백: PR은 발행용 `build-and-push`의 Buildx(docker-container driver) 빌드와 metadata/labels 단계를 실행하지 않으므로, 그 경로에만 있는 실패는 merge 후 dev/main push에서 처음 드러난다. `needs: test` 뒤에서 실패하면 이미지가 발행되지 않으므로 fail-closed이며, 이 post-merge 검출을 의도적으로 수용한다. 스캔 잡의 docker driver 경로(`setup-buildx-action` 없음, `load: true`)도 이 변경 전에는 어디서도 실행되지 않았다. 첫 PR 실행과 merge 후 첫 dev push 실행을 두 경로의 검증으로 삼고 `Build drover-api image target`, `Set up Docker Buildx`, `Build and push API`/`Build and push Worker` 스텝 시간을 기록한다. Buildx 전용 회귀가 반복되면 PR 전용 Buildx dry build의 비용을 실측으로 비교한 뒤 추가를 결정한다.

### 규칙

1. **측정 먼저, 추정 금지.** CI를 바꾸기 전과 후에 최근 20회 이상 실행의 잡·스텝 시간을 `gh run list --workflow ci.yml`, `gh run list --workflow docker-build.yml`, `gh api repos/openstack-afterglow/drover/actions/runs/<id>/jobs`로 수집한다. 크리티컬 패스(실행 생성부터 마지막 필수 잡 종료까지)의 중앙값과 p90을 커밋 본문 또는 PR에 남긴다. 절감 효과는 합산되지 않으므로 가장 긴 잡(현재 `docker-build-and-scan`)부터 줄이고, 실제 CI 전후 수치로만 효과를 주장한다. 로그에서 추정한 값은 투영치로 표시한다.
2. **목표 지표를 먼저 정한다.** Drover는 public 저장소이고 GitHub-hosted runner만 쓰므로 wall-clock(대기 시간)이 목표다. private으로 바꾸거나 유료 runner를 도입하면 runner-minutes(비용)도 함께 본다.
3. **게이트 잡을 다른 잡 앞에 두지 않는다.** `python3 scripts/check_architecture.py` 같은 fail-fast 검사는 `service` 잡의 checkout 다음 첫 스텝으로 두고, 다른 잡의 `needs:`로 걸지 않는다. 이미지·패키지 발행(push)과 배포는 테스트 워크플로우 전체 결과로 게이팅한다. GHCR 이미지 발행은 `build-and-push`의 `needs: test`로 이를 지킨다. 아무것도 발행하지 않는 PR 검증 빌드는 테스트와 병렬로 돌려도 되며, `ci.yml`의 `docker-build-and-scan`(`load: true` 빌드와 Trivy 스캔, push 없음)은 이 조건을 지킨다. 이 CI 변경 전부터 있는 공백: `release.yml`은 `v*` tag push에서 wheel을 빌드해 GitHub Release에 올리지만 suite 결과를 기다리지 않는다(같은 tag의 `Docker Build & Push / test`와 따로 실행된다). 이 변경은 `release.yml`을 바꾸지 않았고, 공백을 닫으려면 release job이 suite 결과를 기다리게 하는 별도 변경이 필요하다.
4. **잡당 고정비를 측정한다.** checkout, `setup-uv`/`uv sync`, Buildx 준비, 서비스 컨테이너 준비 시간을 잰다. 캐시 복원이 재설치보다 느리면 캐시를 쓰지 않는다. 서비스 컨테이너 health-check는 짧은 interval(2초)과 충분한 대기 창(start-period + interval×retries ≥ 30초)으로 설정한다. Docker는 start-period 동안의 probe 실패를 retries에 세지 않으므로 두 창이 더해지며, 계약 테스트는 초 단위(`--health-start-period=Ns`) start-period만 더한다. 단, runner는 `Waiting for all services to be ready`에서 `docker inspect` health 상태를 늘어나는 backoff로 다시 확인한다(관측 대기 2→4→7초 job 105291927609, 2→3→9초 job 105794061515, 둘 다 이전 10초 interval). 확인 시점이 대기 시작 후 약 0/2/6/14초(다음은 약 30초, 투영)라 interval 단축의 절감은 확인 시점 단위로 양자화된다(10초 interval에서 약 14초 확인에 걸리던 healthy가 약 6초 또는 2초 확인으로 당겨지면 약 8초 또는 12초, 아니면 0초). 두 실행에서 MariaDB healthy는 약 14초 시점 확인에서 관측됐으므로, 2초 interval의 절감은 `Initialize containers` 스텝 시간을 전후 실측하기 전까지 미확인으로 둔다. 스캔 전용 이미지 빌드는 default docker driver로 daemon에 직접 빌드해 BuildKit container boot와 `load: true` tarball export/import를 피한다. docker driver는 GHA build cache를 export할 수 없으므로, layer cache를 도입하려면 이 선택을 실측으로 비교한 뒤 바꾼다.
5. **샤딩은 고정비가 작을 때만 한다.** 테스트 러너가 작업을 나누는 단위(pytest는 파일/test item)로 균형을 맞춘다. 샤드 명령은 러너를 직접 호출한다. 래퍼 스크립트 뒤에 샤드 인자를 붙이면 전달되지 않아 전체 스위트가 조용히 돌 수 있으므로, 샤드별 실행 수를 CI에서 검증한다. 현재 `service` pytest는 로컬 11-18초(실측, macOS arm64)이고, CI 값은 개선 후 아직 실측하지 않았다(투영치: 기준선 pytest 스텝 78초에서 제거한 고정 대기 약 60초의 sleep과 최대 약 3.5초의 TLS 접속 timeout을 뺀 약 15-20초). 어느 쪽이든 샤딩 대상이 아니며, 실측이 쌓이면 이 값을 바꾼다.
6. **격리 해제는 opt-in으로만 한다.** 워커 간 상태 공유나 isolation 해제 같은 최적화는 전역에 적용하지 않는다. 먼저 순서를 섞어(shuffle) 2회 이상 실행해 상태 누수를 확인하고, 안전한 파일만 명시적으로 opt-in한다. monkeypatch·전역 상태를 바꾼 테스트는 반드시 복원한다.
7. **테스트는 hermetic해야 병렬화할 수 있다.** 단위 테스트는 실제 외부 서비스(Keystone, OpenStack API, K3s API server, 임의 IP의 TLS endpoint 등)에 접속하지 않는다. keepalive loop는 완료를 늦추지 않는다. 기다리던 작업이 끝나면 keepalive 간격을 채우지 않고 진행한다(예: `drover/services/cert_rotation.py`의 `_NODE_READY_KEEPALIVE_SECONDS`). 그 밖의 운영 대기(settle, backoff, retry 간격 등)는 이름 있는 모듈 상수로 두고 테스트가 0으로 patch하며, 운영 값을 바꾸거나 없애려면 CI 속도와 별개인 자체 안전 근거가 필요하다. 2026-09 cert rotation 변경은 keepalive loop와 함께 control-plane restart 사이의 암묵적 10초 하한도 없앴다. 그 안전 근거는 `ARCHITECTURE.md` Certificate rotation 절에 있고 owner 확인은 아직 받지 않았다. 외부 접속 실패 경로는 `socket.create_connection` 같은 경계를 patch해 실행한다(예: `tests/test_k3s_certs.py`의 TLS probe 실패 테스트). 2026-09-24 전체 suite를 non-loopback `socket.connect` recorder로 돌렸을 때 0건이었다. 로컬 설정 파일 유무에 따라 결과나 시간이 달라지면 결함이다. pytest-xdist 등 병렬 실행을 도입하면 워커 수를 CI vCPU에 맞춰 명시한다(`-n auto` 금지).
8. **변경 감지의 diff 기준을 정확히 한다.** 현재 Drover CI에는 paths filter나 변경 감지가 없다. 도입하면 push는 `github.event.before..github.sha`로 비교하고, zero SHA·forced push·fetch 실패 시에는 전체를 대상으로 한다. PR은 base..head로 비교한다. `HEAD^1..HEAD`처럼 마지막 커밋만 보는 비교는 금지한다. 발행 산출물(이미지 등)은 실제 발행된 revision을 기준으로 판단한다.
9. **중복 실행은 입력 동일성으로만 제거한다.** 같은 이벤트·같은 SHA에서 같은 `ci.yml`이 두 번 도는 경우(예: 이전의 push 시 `CI`와 `Docker Build & Push / test`)는 트리거 구성으로 제거한다. PR 테스트를 건너뛰는 최적화는 같은 저장소의 브랜치에서 온 PR이고 merge 트리가 이미 테스트된 head 트리와 같을 때만 허용한다. fork PR과 dependabot PR은 항상 테스트한다. 브랜치 이름만으로 판단하지 않는다(fork의 동명 브랜치 우회). 현재 `ci.yml`의 잡과 스텝에는 `if:`가 없고 계약 테스트가 이를 고정한다. `github.actor`, `event_name`, 브랜치 이름, 또는 tree 동일성 확인 없는 `head.repo` 비교만으로 skip하지 않는다. skip된 잡·스텝은 성공으로 보고되므로, 건너뛰기를 도입하려면 위 동일성 조건을 구현하고 계약 테스트를 같은 변경에서 명시적으로 바꾼다.
10. **보안: public 저장소의 `pull_request` 코드를 self-hosted runner에서 실행하지 않는다.** 현재 모든 잡은 GitHub-hosted `ubuntu-latest`다. PR 코드를 실행하는 workflow(PR trigger가 있는 파일과 그 파일이 호출하는 local reusable workflow)는 문자열 `ubuntu-*` runner와 top-level `permissions: contents: read`만 쓰고, job-level `permissions`·`environment`·`secrets`(호출하는 workflow에 전달)를 두지 않으며, `secrets.GITHUB_TOKEN` 외의 `secrets` 참조(`toJSON(secrets)` 포함)를 쓰지 않도록 계약 테스트가 고정한다. 워크플로우 YAML의 `if:`·`runs-on`과 이 계약 테스트는 PR이 함께 수정할 수 있으므로, 설정 수준 통제로도 보장한다. 2026-09-24 읽기 전용 `gh api`로 확인한 상태와 경로:
    - fork PR 승인: repository Settings → Actions → General → "Approval for running fork pull request workflows from contributors"(`https://github.com/openstack-afterglow/drover/settings/actions`). 값은 `first_time_contributors`(`gh api repos/openstack-afterglow/drover/actions/permissions/fork-pr-contributor-approval`)(UI의 "Require approval for first-time contributors")라 이 저장소에 처음 기여하는 사용자의 fork PR만 승인을 기다린다. 더 엄격한 `all_external_contributors`로 바꿀지는 owner 결정이다.
    - runner group 저장소 제한: Drover는 organization `openstack-afterglow` 소유라 org runner group이 적용된다. repository 수준 self-hosted runner는 0개였다(`gh api repos/openstack-afterglow/drover/actions/runners`). org runner group 목록은 `admin:org` 권한이 없어 읽지 못했으므로(HTTP 403), self-hosted runner group이 Drover에 열려 있지 않은지(group별 "Repository access"와 "Allow public repositories") 확인·유지하는 것은 org owner 작업이다: Organization settings → Actions → Runner groups(`https://github.com/organizations/openstack-afterglow/settings/actions/runner-groups`). self-hosted runner를 도입하는 변경은 두 설정을 같은 변경에서 확인하고 이 항목을 갱신한다.
11. **CI 형태는 계약 테스트로 고정한다.** 트리거 구성(PR은 `CI`, push/tag는 `Docker Build & Push / test`, `docker-build.yml` push 필터는 `branches`·`tags`만), fail-closed 발행 게이트(`docker-build.yml`에서 `test`를 뺀 잡 중 `packages: write`·`write-all` 권한, `docker/login-action`, literal `false`가 아닌 `push`의 `docker/build-push-action` 중 하나라도 있는 모든 잡의 `needs`에 `test` 포함, 그 잡들과 `test`에 job-level `if`·`continue-on-error` 없음, `ci.yml` 잡·스텝에 `continue-on-error` 없음), `ci.yml` 잡·스텝의 무조건 실행과 잡 간 `needs` 없음(3·9번), PR 코드의 runner·token·secret 제한(10번), `service`의 checkout 다음 첫 스텝 `python3 scripts/check_architecture.py`와 정확한 `uv run pytest tests`, 빌드한 모든 이미지의 Trivy 스캔, 스캔 잡의 docker driver와 `load: true`, 서비스 health-check interval과 window(start-period + interval×retries, 4번), Trivy pin 같은 불변식은 `tests/test_ci_workflows.py`에서 검증한다. 3번의 `release.yml` 공백과 10번의 설정 수준 통제는 이 테스트가 고정하지 않는다. 워크플로우를 바꾸면 이 테스트를 같은 변경에서 갱신한다.
12. **지속 개선.** CI를 바꾸는 변경에는 전후 실측을 첨부한다. 크리티컬 패스 중앙값이 가장 최근에 기록된 같은 이벤트의 기준선보다 20% 이상 나빠지거나(현재 기준 `CI` PR 125초 초과, `Docker Build & Push` push suite 구간 118초 초과; 혼합 `CI` 기준 120초는 참고용), 테스트 수가 크게 늘거나, 새 테스트 계층을 추가하면 1번 절차로 다시 측정하고 가장 긴 잡부터 개선한다. 보류된 후보와 보류 이유: drover-worker 이미지의 중복 Trivy 스캔(중앙값 18초, 두 target 스캔 계약), Dockerfile apt layer cache(p90 tail의 원인, docker driver와 상충하므로 실험 브랜치에서 측정), pytest-xdist(`service`가 크리티컬 패스가 아니게 됨), main merge·tag 재테스트 생략(발행 게이팅 위험).
